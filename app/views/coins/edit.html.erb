<h1 class="title_coin"><%= t('editing.coin') %></h1>

<%= render 'form', coin: @coin %>

<%= link_to t('links.show'), @coin, class: "btn btn-dark" %> |
<%= link_to t('links.back'), coins_path, class: "btn btn-dark" %>
